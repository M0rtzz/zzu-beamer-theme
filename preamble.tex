\usepackage{etoolbox}
\makeatletter
\csdef{ver@transparent.sty}{114514/01/01}
\providecommand{\transparent}[1]{}
\makeatother
\usepackage{fontspec}
\usepackage[fontset=none]{ctex}
\usepackage[dvipsnames, svgnames, x11names]{xcolor}
\usepackage[comma, numbers, sort&compress, square]{natbib}
\usepackage{minted}
\usemintedstyle{manni}
\setminted{
    breaklines=true, % 自动换行
    breakautoindent=false, % 换行不保持缩进
    frame = single, % 添加单线边框
    fontsize=\footnotesize, % 9pt 字体
    linenos, % 启用行号显示
    numbersep=5pt, % 行号间距 5pt
    tabsize=4 % 制表符为 4 空格
}
\usepackage{
    svg,
    xurl,
    ulem,
    forest,
    xpatch,
    amsmath,
    caption,
    booktabs,
    calligra,
    hyperref,
    graphicx,
    listings,
    tabularx,
    adjustbox,
    tcolorbox,
    xltabular
}
\usepackage{unicode-math}
\setmathfont{TeX Gyre Termes Math}
\AtBeginDocument{\renewcommand{\mathbf}{\symbf}}
\allowdisplaybreaks
\usepackage{ZZU}

\usepackage{luacode}
\begin{luacode*}
    luaotfload.add_fallback("emojifallback", {
            "[fonts/TwitterColorEmoji.otf]:mode=harf;"
    })
\end{luacode*}

\setmainfont{Times New Roman}[
    Path = fonts/,
    UprightFont = times.ttf,
    BoldFont = timesbd.ttf,
    ItalicFont = timesi.ttf,
    BoldItalicFont = timesbi.ttf,
    RawFeature = {fallback=emojifallback}
]
\setsansfont{Times New Roman}[
    Path = fonts/,
    UprightFont = times.ttf,
    BoldFont = timesbd.ttf,
    ItalicFont = timesi.ttf,
    BoldItalicFont = timesbi.ttf,
    RawFeature = {fallback=emojifallback}
]
\setmonofont{FiraCode}[
    Path = fonts/,
    UprightFont = FiraCode-Regular.ttf,
    BoldFont = FiraCode-Bold.ttf,
    ItalicFont = FiraCode-Italic.ttf,
    BoldItalicFont = FiraCode-BoldItalic.ttf,
    RawFeature = {fallback=emojifallback}
]

\setCJKmainfont{SimHei}[
    Path = fonts/,
    UprightFont = SimHei.ttf,
    BoldFont = SimHei.ttf,
    BoldFeatures = {FakeBold=2},
    ItalicFont = SimHei.ttf,
    ItalicFeatures = {FakeSlant=0.2},
    BoldItalicFont = SimHei.ttf,
    BoldItalicFeatures = {FakeBold=2, FakeSlant=0.2}
]
\setCJKsansfont{SimHei}[
    Path = fonts/,
    UprightFont = SimHei.ttf,
    BoldFont = SimHei.ttf,
    BoldFeatures = {FakeBold=2},
    ItalicFont = SimHei.ttf,
    ItalicFeatures = {FakeSlant=0.2},
    BoldItalicFont = SimHei.ttf,
    BoldItalicFeatures = {FakeBold=2, FakeSlant=0.2}
]
\setCJKmonofont{LXGWWenKaiMono}[
    Path = fonts/,
    UprightFont = LXGWWenKaiMono-Regular.ttf,
    BoldFont = LXGWWenKaiMono-Regular.ttf,
    BoldFeatures = {FakeBold=2},
    ItalicFont = LXGWWenKaiMono-Regular.ttf,
    ItalicFeatures = {FakeSlant=0.2},
    BoldItalicFont = LXGWWenKaiMono-Regular.ttf,
    BoldItalicFeatures = {FakeBold=2, FakeSlant=0.2}
]
\newCJKfontfamily\wuyushengxingkai{wuyushengxingkai}[
    Path = fonts/,
    UprightFont = 吴玉生行楷.otf
]

\renewcommand{\bibsection}{}
\newcommand{\temp}[1]{\textcolor{blue}{\uline{\color{blue}#1}}}
\newcommand{\blueurl}[2][blue]{\href{#2}{\temp{#2}}}

\def\cmd#1{\texttt{\color{red}\footnotesize $\backslash$#1}}
\def\env#1{\texttt{\color{blue}\footnotesize #1}}
\definecolor{deepblue}{rgb}{0,0,0.5}
\definecolor{deepred}{rgb}{0.6,0,0}
\definecolor{deepgreen}{rgb}{0,0.5,0}
\definecolor{halfgray}{gray}{0.55}

\lstset{
    basicstyle=\ttfamily\small,
    keywordstyle=\bfseries\color{deepblue},
    emphstyle=\ttfamily\color{deepred},
    stringstyle=\color{deepgreen},
    numbers=left,
    numberstyle=\small\color{halfgray},
    rulesepcolor=\color{red!20!green!20!blue!20},
    frame=shadowbox,
}

\setcounter{tocdepth}{4}
\setcounter{secnumdepth}{4}

\forestset{
    default preamble={
        for tree={
            grow=south,
            parent anchor=south,
            child anchor=north,
            fit=band,
            % s sep=0.8em, % sibling separation
            % l sep=2.5em, % level separation
            edge={-stealth},
            font=\sffamily\small,
            text centered,
            draw,
            rounded corners=3pt,
            edge path={
                \noexpand\path[\forestoption{edge}]
                (\forestOve{\forestove{@parent}}{name}.south) --
                (\forestove{name}.north);
            },
        },
    },
    selected/.style={
        fill=zzu,
        text=white,
        thick,
    },
}

\tcbset{zzubox/.style={
        colback=zzu!5,
        colframe=zzu,
        coltitle=white,
        colbacktitle=zzu,
        fonttitle=\bfseries,
        left=1mm, right=1mm, top=1mm, bottom=1mm,
        boxrule=0.8pt,
        arc=2pt
}}
